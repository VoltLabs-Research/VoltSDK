"""Integration modules for third-party libraries.

These are lazily imported so that optional dependencies (matplotlib,
OVITO, VTK/k3d) are only required when the corresponding functions
are actually called.
"""
